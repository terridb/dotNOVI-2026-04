apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Service name on the monitoring network (NOT localhost - that's the Grafana container)
    url: http://prometheus:9090
    uid: prometheus
    isDefault: true
    editable: true